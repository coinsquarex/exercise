# Prerequisites

Use the verison of node defined in `.node-version`.

## Install dependencies

```sh
# /balance
npm install

# /frontend
npm install
```

## Run the application

```sh
# /balance
npm run start:dev

# /frontend
npm run start:dev
```